<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Rumuo launcher uses the bird mark without a colored background. -->
    <color name="ic_launcher_background">#00000000</color>

    <!-- Pre-Flutter splash background — LIGHT MODE (day qualifier).
         Must match AppTheme.lightBg (#FFFFFF) so the native → Flutter
         transition is imperceptible on light-theme devices. -->
    <color name="launch_background">#FFFFFF</color>
</resources>
